<Window x:Class="SerialWorkbench.WinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Window.SystemBackdrop>
    <MicaBackdrop Kind="Base" />
  </Window.SystemBackdrop>
  <Grid x:Name="RootGrid">
    <Grid.RowDefinitions>
      <RowDefinition Height="48" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <TitleBar x:Name="AppTitleBar"
        Title="SERIAL / WORKBENCH"
        IsBackButtonVisible="False"
        IsPaneToggleButtonVisible="True"
        PaneToggleRequested="AppTitleBar_PaneToggleRequested">
      <TitleBar.IconSource>
        <ImageIconSource ImageSource="/Assets/SerialWorkbench.ico" />
      </TitleBar.IconSource>
    </TitleBar>
    <NavigationView x:Name="Navigation"
        Grid.Row="1"
        IsBackButtonVisible="Collapsed"
        IsSettingsVisible="False"
        IsPaneToggleButtonVisible="False"
        SelectionChanged="Navigation_SelectionChanged">
      <NavigationView.MenuItems>
        <NavigationViewItem Content="接收" Icon="View" Tag="Receive" />
        <NavigationViewItem Content="Modbus RTU" Icon="AllApps" Tag="Modbus" />
        <NavigationViewItem Content="回环检测" Icon="Refresh" Tag="Loopback" />
        <NavigationViewItem Content="会话记录" Icon="Clock" Tag="Sessions" />
      </NavigationView.MenuItems>
      <NavigationView.FooterMenuItems>
        <NavigationViewItem Content="设置" Icon="Setting" Tag="Settings" />
      </NavigationView.FooterMenuItems>
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <InfoBar x:Name="ErrorInfoBar" Margin="36,18,36,0" IsOpen="False" IsClosable="True" />
        <Frame x:Name="ContentFrame" Grid.Row="1" IsNavigationStackEnabled="False" Navigated="ContentFrame_Navigated" NavigationFailed="ContentFrame_NavigationFailed" />
      </Grid>
    </NavigationView>
  </Grid>
</Window>
