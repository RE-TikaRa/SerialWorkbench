<Window x:Class="SerialWorkbench.WinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Window.SystemBackdrop>
    <MicaBackdrop Kind="Base" />
  </Window.SystemBackdrop>
  <Grid x:Name="RootGrid">
    <Grid.RowDefinitions>
      <RowDefinition Height="48" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <TitleBar x:Name="AppTitleBar"
        Title="SERIAL / WORKBENCH"
        IsBackButtonVisible="False"
        IsPaneToggleButtonVisible="True"
        PaneToggleRequested="AppTitleBar_PaneToggleRequested">
      <TitleBar.IconSource>
        <ImageIconSource ImageSource="/Assets/SerialWorkbench.ico" />
      </TitleBar.IconSource>
    </TitleBar>
    <NavigationView x:Name="Navigation"
        Grid.Row="1"
        PaneDisplayMode="Left"
        OpenPaneLength="248"
        IsBackButtonVisible="Collapsed"
        IsSettingsVisible="False"
        IsPaneToggleButtonVisible="False"
        SelectionChanged="Navigation_SelectionChanged">
      <NavigationView.PaneHeader>
        <StackPanel Margin="4,4,4,12" Spacing="4">
          <TextBlock Text="SerialWorkbench" FontSize="20" FontWeight="SemiBold" />
          <TextBlock Text="串口工具" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </StackPanel>
      </NavigationView.PaneHeader>
      <NavigationView.MenuItems>
        <NavigationViewItem Content="接收" Icon="View" Tag="Receive" />
        <NavigationViewItem Content="回环检测" Icon="Refresh" Tag="Loopback" />
        <NavigationViewItem Content="会话记录" Icon="Clock" Tag="Sessions" />
      </NavigationView.MenuItems>
      <NavigationView.FooterMenuItems>
        <NavigationViewItem Content="设置" Icon="Setting" Tag="Settings" />
      </NavigationView.FooterMenuItems>
      <NavigationView.PaneFooter>
        <Border Margin="4,8"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="12">
          <StackPanel Spacing="7">
            <TextBlock Text="服务状态" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            <StackPanel Orientation="Horizontal" Spacing="8">
              <Ellipse x:Name="StatusIndicator" Width="8" Height="8" VerticalAlignment="Center" Fill="{ThemeResource SystemFillColorNeutralBrush}" />
              <TextBlock x:Name="ConnectionStatusText" Text="正在连接 Host…" FontSize="12" />
            </StackPanel>
            <TextBlock x:Name="CountersText" Text="RX 0 · TX 0" FontFamily="{StaticResource MonospaceFontFamily}" FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
          </StackPanel>
        </Border>
      </NavigationView.PaneFooter>
      <Frame x:Name="ContentFrame" NavigationFailed="ContentFrame_NavigationFailed" />
    </NavigationView>
    <InfoBar x:Name="HostInfoBar" Grid.Row="1" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,18,24,0" IsOpen="False" IsClosable="True" Width="360" />
  </Grid>
</Window>
