<Window x:Class="SerialWorkbench.WinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Window.SystemBackdrop>
    <MicaBackdrop Kind="Base" />
  </Window.SystemBackdrop>
  <Grid x:Name="RootGrid">
    <Grid.RowDefinitions>
      <RowDefinition Height="48" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <TitleBar x:Name="AppTitleBar"
        Title="SERIAL / WORKBENCH"
        IsBackButtonVisible="False"
        IsPaneToggleButtonVisible="True"
        PaneToggleRequested="AppTitleBar_PaneToggleRequested">
      <TitleBar.Resources>
        <x:Double x:Key="TitleBarPaneToggleButtonWidth">48</x:Double>
        <x:Double x:Key="TitleBarLeftPaddingWidth">0</x:Double>
      </TitleBar.Resources>
      <TitleBar.IconSource>
        <ImageIconSource ImageSource="/Assets/SerialWorkbench.ico" />
      </TitleBar.IconSource>
    </TitleBar>
    <NavigationView x:Name="Navigation"
        Grid.Row="1"
        IsBackButtonVisible="Collapsed"
        IsSettingsVisible="False"
        IsPaneToggleButtonVisible="False"
        SelectionChanged="Navigation_SelectionChanged">
      <NavigationView.MenuItems>
        <NavigationViewItem Content="工作台" Tag="Workbench">
          <NavigationViewItem.Icon>
            <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xE88E;" />
          </NavigationViewItem.Icon>
        </NavigationViewItem>
        <NavigationViewItem Content="终端" Icon="Keyboard" Tag="Terminal" />
        <NavigationViewItem Content="Modbus RTU" Icon="List" Tag="Modbus" />
        <NavigationViewItem Content="协议帧" Icon="Document" Tag="ProtocolInspector" />
        <NavigationViewItem Content="回环检测" Icon="RepeatAll" Tag="Loopback" />
        <NavigationViewItem Content="会话记录" Icon="Clock" Tag="Sessions" />
      </NavigationView.MenuItems>
      <NavigationView.FooterMenuItems>
        <NavigationViewItem Content="设置" Icon="Setting" Tag="Settings" />
      </NavigationView.FooterMenuItems>
      <Grid x:Name="ContentShell" SizeChanged="ContentShell_SizeChanged">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel x:Name="PageHeader" Margin="24,24,24,0" Spacing="6">
          <TextBlock x:Name="PageTitleText" Text="工作台" Style="{StaticResource TitleTextBlockStyle}" />
          <TextBlock x:Name="PageDescriptionText" Text="连接串口、收发报文并查看实时波形。" Style="{StaticResource PageDescriptionStyle}" />
        </StackPanel>
        <InfoBar x:Name="ErrorInfoBar" Grid.Row="1" Margin="24,18,24,0" IsOpen="False" IsClosable="True" />
        <Frame x:Name="ContentFrame" Grid.Row="2" IsNavigationStackEnabled="False" Navigated="ContentFrame_Navigated" NavigationFailed="ContentFrame_NavigationFailed" />
      </Grid>
    </NavigationView>
  </Grid>
</Window>
