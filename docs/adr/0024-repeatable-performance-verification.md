# 性能指标使用可复现测量协议

吞吐、真实回环和自动响应延迟只在 Release win-x64 发布产物、无调试器条件下验收。测试记录系统、硬件、驱动、串口参数和资源序列，先预热 30 秒，再运行三个独立样本。吞吐与回环样本持续 10 分钟；延迟样本至少包含 100000 次响应，并使用同一单调时钟测量成帧确认到发送请求进入调度器的时间。每项结果保留字节一致性、p50/p95/p99/最大值、CPU、内存、GC、队列水位和 SQLite 提交证据，使 5 MiB/s、3 Mbaud 与 20 ms p99 可以重复验证。
