# 程序数据位于 EXE 同目录或当前工作区

应用根目录是 `SerialWorkbench.exe` 所在目录。未打开工作区时，设置、资料库、全局会话、扩展登记和缓存保存在其 `data/` 子目录；打开工作区后，工作区资产、会话、报告和任务视图状态保存在工作区内。程序不使用 AppData、LocalAppData、ProgramData 或其他隐式系统目录。免安装版可以整体迁移，安装版必须使用可写安装目录，升级默认保留 `data/`。只有用户显式打开工作区、导出或另存为时才向应用根目录之外写入文件。
