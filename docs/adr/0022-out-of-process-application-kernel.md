# 功能内核运行在独立 Host 进程

`SerialWorkbench.Host` 独立进程拥有连接、写入租约、任务、活动会话、持久化和协议扩展宿主生命周期。WinUI 与 CLI 通过版本化本机 IPC 使用同一应用内核，使多个窗口和命令行调用共享一致状态，并使界面故障不损坏正在写入的会话。Host 按需启动，不安装为系统服务，也不在通知区域常驻。最后客户端正常退出时执行活动工作确认与有序清理；异常断开后保留 30 秒重连窗口。托管任务在异常断开后继续并保留任务租约，没有托管任务时在宽限期结束后关闭连接、完成会话并退出。活动会话本身不构成无限驻留条件。
