# 通用界面只使用 WinUI 3 原生控件

按钮、导航、标签页、列表、表格、树、表单、对话框、滚动、状态提示和文本呈现使用 WinUI 3 与 Windows App SDK 原生控件。同类交互集中为共享 XAML 控件组合、样式、数据模板和命令，不在各任务文档重复实现。通用界面不使用自绘 Canvas、Win2D、Direct2D、仿 WinUI 控件或第三方控件库。时间序列、XY、直方图和 FFT 允许使用 `ScottPlot.WinUI` 作为数据画布，这项例外不扩展到通用交互组件。ANSI/VT 终端由原生文本与虚拟化列表控件组合呈现。
