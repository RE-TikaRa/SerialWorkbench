# 领域可视化使用受信任的 WinUI 扩展

3D、地图、雷达和图像等领域视图由显式安装并受信任的 WinUI 扩展提供。扩展在界面进程中加载，以获得 WinUI 输入和无障碍能力，公共扩展 API 只暴露数据通道、只读会话查询和界面选择状态。该边界属于受信任代码合约和架构依赖约束，同进程代码不具备安全隔离，恶意扩展仍拥有当前用户进程能力。连接、写入租约、自动化执行器和会话写入器始终位于独立 Host；协议扩展仍运行在独立扩展宿主中。
