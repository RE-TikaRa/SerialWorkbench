# 测试序列使用结构化并行组

多设备联合测试通过具有明确起点、汇合点和共同生命周期的并行组表达。每个分支从父作用域读取变量快照，在独立作用域内写入，并通过声明的确定性归并规则导出变量。分支必须预先声明连接读写需求，重叠的写入需求默认验证失败；确需共享连接时使用显式串行化策略。分支持有写入租约直到自己的清理段结束。并行组默认在首个分支失败时取消其他分支并执行清理段，也可等待全部分支；清理失败归为运行错误，其余结果按运行错误、超时、验证失败、取消、通过归并。全局顺序执行无法表达同时采集与联合触发，任意后台步骤又难以取消、合并和追踪，因此序列只允许结构化并行。
