# RS-485 半双工方向由串口层统一管理

RS-485 连接资料在适配器或驱动明确提供自动方向能力时选择自动模式，否则选择 RTS 方向控制。RTS 模式同时声明有效极性、发送前延时、实际写入完成后的保持时间、总线静默间隔和本地回显抑制。串口层以驱动报告的写入完成作为收发切换基准，并把方向切换、静默间隔不足、回显、冲突和超时写入统一会话。Windows 没有供所有适配器共用的 RS-485 自动方向保证，因此 Modbus、测试序列和原始发送都复用连接暴露的实际能力，不各自实现收发切换逻辑。
