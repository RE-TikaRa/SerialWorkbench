# 串口工作台

面向多串口设备调试、协议分析、自动化测试和回环检测的统一领域。功能内核管理连接、数据、任务和记录，图形界面与命令行只是不同宿主。

## Language

**连接**：
一次活动的串口通信端点，拥有独立参数、生命周期、收发管线和统计。
_Avoid_: 当前串口、页面串口

**监视源**：
向会话提供串口活动但不拥有该串口写入权的数据来源。
_Avoid_: 只读连接、抓包连接

**连接角色**：
测试资产使用的稳定逻辑名称，在运行时映射到实际连接。
_Avoid_: 固定 COM 口、端口编号

**运行映射**：
一次任务运行中经过确认的连接角色与实际连接对应关系。
_Avoid_: 端口绑定、默认串口

**连接资料**：
全局资料库中用于识别设备并预填串口参数的可复用记录。
_Avoid_: 默认连接、设备配置

**写入租约**：
一项运行任务在指定连接上获得的排他发送权限。
_Avoid_: 串口锁、任务锁

**任务**：
使用一条或多条连接执行监视、发送、测试、传输或回环检测的运行单元。
_Avoid_: 页面、工作区任务

**终端任务**：
解释 ANSI/VT 控制序列、维护终端屏幕状态并向连接发送交互输入的任务。
_Avoid_: 文本监视、控制台日志

**测试序列**：
由连接、发送、等待、验证、控制和记录步骤组成的声明式自动化资产。
_Avoid_: 脚本、宏

**并行组**：
在测试序列中同时执行多个受共同生命周期约束的步骤分支。
_Avoid_: 后台任务、异步脚本

**分支作用域**：
并行分支从父作用域读取变量快照并保存自己的变量写入，直到汇合时才处理显式导出。
_Avoid_: 共享变量、全局变量

**导出归并**：
并行分支把声明的结果变量按确定规则写回父作用域的汇合操作。
_Avoid_: 最后写入、自动合并

**清理段**：
任务、序列或并行分支结束时执行的设备复位与资源释放步骤集合。
_Avoid_: finally、退出脚本

**运行结果**：
测试运行的最终分类，取值为通过、验证失败、运行错误、超时或取消。
_Avoid_: 成功状态、退出状态

**步骤尝试**：
测试步骤的一次具体执行及其发送、响应、耗时和结果记录。
_Avoid_: 重试次数、循环项

**工作区**：
保存特定设备或测试项目资产与记录的可迁移目录。
_Avoid_: 页面、当前视图、标签页

**资产文档**：
带有明确模式版本并保存在工作区或资料库中的 JSON 测试资产。
_Avoid_: 配置文件、项目数据库

**资料库**：
应用范围内保存可复用连接资料、命令、协议模板和测试序列的集合。
_Avoid_: 全局工作区

**全局会话库**：
保存未归属于工作区的会话的应用级目录。
_Avoid_: 临时目录、默认工作区

**会话**：
按统一时间线记录一条或多条连接、任务和用户操作的通信过程。
_Avoid_: 单端口日志、页面日志

**活动会话**：
首次连接或任务启动时自动创建并持续接收当前运行事件的会话。
_Avoid_: 录制模式、当前日志

**会话文件**：
保存一个会话全部原始数据、事件、解析版本和索引的独立 SQLite 文件。
_Avoid_: 日志文件、全局数据库

**会话系列**：
由自动分段产生并保持连续时间关系的一组会话。
_Avoid_: 分卷日志、多个数据库

**记录完整性**：
活动连接产生的全部原始数据和运行事件都能在所属会话中追溯的性质。
_Avoid_: 日志完整、无丢包

**事件时间**：
由 UTC、进程单调时间和递增序号共同表示的会话事件时序。
_Avoid_: 接收时间、系统时间

**读取块**：
一次底层读取返回的原始字节，仅表示 I/O 边界。
_Avoid_: 数据包、报文、帧

**协议帧**：
字节流依据成帧规则划分出的协议报文。
_Avoid_: 读取块、串口包

**数据通道**：
由协议字段或数据转换产生并带有名称、单位和时间的连续值序列。
_Avoid_: 曲线、波形数据

**可视化扩展**：
显式安装并受信任的领域专用视图，只读取数据通道、会话查询和界面选择状态。
_Avoid_: 协议扩展、数据解析器

**协议模板**：
描述成帧规则、字段、校验和请求生成方式的可迁移资产。
_Avoid_: 协议脚本

**协议扩展**：
在独立扩展宿主中提供自定义成帧、解析、生成或测试动作的受信任组件。
_Avoid_: 插件脚本、协议 DLL

**扩展宿主**：
隔离运行协议扩展并通过版本化接口与功能内核通信的独立进程。
_Avoid_: 插件进程、脚本引擎

**扩展引用**：
工作区中声明协议扩展标识、兼容版本范围和配置的非可执行记录。
_Avoid_: 内置插件、随项目 DLL

**解析版本**：
协议模板快照与解析器版本共同产生的一组会话解析结果。
_Avoid_: 最新解析、缓存结果

**发送项**：
包含待生成内容、转换规则和执行条件的一次可调度发送定义。
_Avoid_: 命令、数据包

**回环检测**：
通过 TX-RX 回接验证串口路径数据完整性、时序和吞吐量的测试任务。
_Avoid_: 多芯线缆检测

**半双工方向控制**：
RS-485 连接在发送与接收之间切换适配器方向的连接能力，包含极性、前后延时、静默间隔和回显处理。
_Avoid_: Modbus 延时、RTS 开关

## Relationships

- 一个**工作区**包含零个或多个**连接角色**、**协议模板**和**测试序列**
- 一个**工作区**包含一个或多个版本化**资产文档**
- 一个**连接角色**在一次运行中通过**运行映射**对应恰好一个**连接**
- 一个**任务**使用一条或多条**连接**
- 一个**终端任务**使用恰好一条**连接**，其原始收发数据进入所属**会话**
- 一条**连接**同时至多授予一个**写入租约**，监视、解析和记录不需要租约
- 一个**会话**可以接收零个或多个**监视源**产生的事件
- 一个**测试序列**包含一个或多个步骤，并可引用多个**连接角色**
- 一个**测试序列**可以包含零个或多个**并行组**，并行分支在显式汇合点结束
- 一个**并行组**默认在首个分支失败时取消其他分支，并在汇合前执行各分支的**清理段**
- 一个并行分支拥有一个**分支作用域**，只有显式**导出归并**可以修改汇合后的父作用域
- 两个并行分支不能隐式持有同一连接的写入租约，重叠写入需求需要显式串行化
- 一个**测试序列**运行产生恰好一个**运行结果**
- 一个步骤产生一个或多个**步骤尝试**，额外尝试必须由该步骤显式声明
- 一个**连接**产生零个或多个**读取块**
- 一个 RS-485 **连接**可以声明一种**半双工方向控制**方式
- 零个或多个**读取块**组成连续字节流，字节流产生零个或多个**协议帧**
- 一个解析字段可以产生零个或多个**数据通道**
- 一个**可视化扩展**消费零个或多个**数据通道**，但不拥有连接、写入租约或会话写入权
- 一个**协议模板**定义**协议帧**的识别、解析和生成规则
- 一个**扩展宿主**运行零个或多个**协议扩展**
- 一个**协议模板**可以引用零个或一个**协议扩展**
- 一个**工作区**可以包含零个或多个**扩展引用**，引用只有在对应扩展已显式安装并启用时生效
- 一个**会话**可以包含一个或多个**解析版本**，所有版本共享同一份原始字节
- 一个**会话**记录一条或多条**连接**及其关联**任务**的统一时间线
- 一个桌面应用实例同时至多拥有一个**活动会话**
- 一个**会话**归属于一个**工作区**或**全局会话库**
- 一个**会话**对应恰好一个可独立迁移的**会话文件**
- 一个**会话系列**包含一个或多个按时间连续的**会话**
- 一个**会话**中的每个事件拥有恰好一个**事件时间**
- 磁盘不足以维持**记录完整性**时，相关任务结束并关闭其使用的连接
- **资料库**中的资产复制到**工作区**后成为独立资产

## Example dialogue

> **开发者：** “测试序列里保存 COM7 吗？”
> **领域专家：** “保存连接角色 DUT。运行时把 DUT 映射到当前实际连接，会话再记录本次使用的设备和 COM 口。”

## Flagged ambiguities

- README 使用“工作区”同时表示导航页面和磁盘目录；领域语言保留**工作区**表示可迁移目录，界面中的打开内容称为任务文档
- README 的 `SerialPacket` 同时承担底层读取和协议报文含义；已拆分为**读取块**与**协议帧**
- README 描述所有功能共享单条连接；产品模型已经改为多条并行**连接**
- README 使用“脚本”描述自动化；内置自动化统一称为**测试序列**，外部程序通过命令行调用
